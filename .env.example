# Pinata API Configuration
# To get these keys, sign up at Pinata.cloud, go to API Keys section and create a new key.
# You can use the JWT (longer) or the API Key and Secret.
# The backend will use PINATA_JWT for the Bearer token authorization.
PINATA_JWT="your_pinata_jwt_here"

# Optional: Dedicated Pinata Gateway (e.g., https://mygateway.mypinata.cloud)
# If not set, it defaults to the public https://gateway.pinata.cloud
PINATA_GATEWAY="https://gateway.pinata.cloud"
